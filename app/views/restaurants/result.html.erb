<h1>レストラン検索結果</h1>

<% if flash[:alert] %>
  <p class="alert"><%= flash[:alert] %></p>
<% end %>

<% if @restaurants && @restaurants[:results][:shop].present? %>
  <ul>
    <% @restaurants[:results][:shop].each do |shop| %>
      <li>
        <%= link_to restaurants_show_path(id: shop[:id]), class: "show_link" do %>
          <strong><%= shop[:name] %></strong><br>
          <img src="<%= shop[:photo][:pc][:l] %>">
          住所: <%= shop[:address] %><br>
          アクセス: <%= shop[:access] %><br>
          詳細: <a href="<%= shop[:urls][:pc] %>" target="_blank">店舗ページ</a>
        <% end %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>検索結果がありません。</p>
<% end %>